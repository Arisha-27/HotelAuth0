# ═══════════════════════════════════════════
# Aegis Hospitality OS - Environment Configuration
# ═══════════════════════════════════════════

# ── Auth0 (Phase 2) ──
AUTH0_DOMAIN=your-tenant.us.auth0.com
AUTH0_CLIENT_ID=your-client-id
AUTH0_CLIENT_SECRET=your-client-secret
AUTH0_AUDIENCE=https://api.aegisresponse.com

# ── Gmail API (Step 56) ──
# Leave blank to use mock mode
# You need: Client ID, Client Secret, and a Refresh Token from Google OAuth2 consent flow
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
GMAIL_SENDER_EMAIL=aegis-os@aegis-hospitality.com

# ── Notion API (Step 57) ──
# Leave blank to use mock mode
NOTION_API_KEY=
NOTION_DATABASE_ID=

# ── Twilio SMS (Step 58) ──
# Leave blank to use mock mode
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=+15551234567

# ── LLM Brain (Phase 4) ──
# Provider: mock (default, no keys), openai, gemini, mistral, ollama, custom
LLM_PROVIDER=mock
LLM_API_BASE=
LLM_API_KEY=
LLM_MODEL=gemini-2.0-flash
